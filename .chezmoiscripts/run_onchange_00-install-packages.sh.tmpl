#!/bin/sh

# {{ .packages }}

tmpdir=$(mktemp -d)

cd "$tmpdir"

curl -fsSL https://astral.sh/uv/install.sh | env UV_UNMANAGED_INSTALL="$tmpdir" sh
XONSH_SHOW_TRACEBACK=true ./uvx --with pydantic xonsh {{ .chezmoi.sourceDir }}/.chezmoiscripts/.install-packages.xsh

rm -rf "$tmpdir"

printf "\nPlease restart your shell.\n"
