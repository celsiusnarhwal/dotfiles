#!/bin/zsh

# {{ .packages }}

tmpdir=$(mktemp -d)

cd "$tmpdir"

curl -fsSL https://astral.sh/uv/install.sh | env UV_UNMANAGED_INSTALL="$tmpdir" sh
XONSH_SHOW_TRACEBACK=true ./uvx --with pydantic xonsh $(chezmoi source-path)/.install-packages.xsh
